name: activity_report_app
description: سامانه گزارشات روزانه و تجمیعی مدیریت بازرسی
publish_to: "none"

version: 2.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.3.2
  file_picker: ^8.1.2
  path_provider: ^2.1.4
  share_plus: ^10.0.2
  intl: ^0.19.0
  csv: ^6.0.0
  pdf: ^3.11.1
  printing: ^5.13.4
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
